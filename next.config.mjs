/**
 * This file is a configuration file for Next.js, a popular React framework.
 * The file exports a `nextConfig` object which is used to customize various aspects of Next.js.
 * Currently, the `nextConfig` object is empty, meaning that the default Next.js configuration is being used.
 *
 * @module next.config
 */
/** filepath: next.config.mjs **/
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
